apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
spec:
  type: NodePort
  selector:
    app: fastapi-hello
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080  # 可透過 http://<VPS_IP>:30080 存取
